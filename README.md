# scroll-prototype-canvas
video animation with scroll drawing frames on canvas

## install
`npm install`

## run
- locally
`npm run dev`

- for testing
`npm run dev -- --host`

## build

`npm run build`
